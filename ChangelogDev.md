# Changelog dev

> This file is used to accumulate changes before a changelog for a release is created.

* Added missing function `bbmod_wrap_value(_value, _rangeMax)`, required in `BBMOD_AnimationPlayer`, which wraps given value to range 0..max-1.
* Fixed macro `BBMOD_RELEASE_PATCH` being the wrong value.
